import 'package:flutter/material.dart';

class SettingPagesGeneral extends StatelessWidget {
  const SettingPagesGeneral({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('General Settings Page'));
  }
}
