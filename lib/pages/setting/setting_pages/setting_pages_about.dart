import 'package:flutter/material.dart';

class SettingPagesAbout extends StatelessWidget {
  const SettingPagesAbout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('About Settings Page'));
  }
}
